{
  "name": "@company/browser-matrix",
  "version": "0.1.0",
  "private": true,
  "description": "Browser support policy: derives the supported browser matrix from the required native feature floor and checks aggregate global usage coverage.",
  "type": "module",
  "scripts": {
    "report": "node ./report.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "browserslist": "catalog:",
    "caniuse-lite": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
