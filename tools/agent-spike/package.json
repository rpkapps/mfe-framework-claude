{
  "name": "@company/agent-spike",
  "version": "0.0.0",
  "private": true,
  "description": "The AG-UI spike that picks the shell's chat library: page actions called by a backend agent through the action pipeline, approvals, and interrupts, against a TanStack AI backend and an Agent Framework (.NET) one. Throwaway: E replaces it.",
  "type": "module",
  "scripts": {
    "spike": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ag-ui/client": "catalog:",
    "@ag-ui/core": "catalog:",
    "@company/mfe-react": "workspace:*",
    "@tanstack/ai": "catalog:",
    "@tanstack/ai-client": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
