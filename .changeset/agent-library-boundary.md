---
'@company/eslint-plugin-mfe': minor
---

The author presets and the `framework` preset reject AI and agent libraries and model provider SDKs, type imports included (§45): `ai`, `openai`, `langchain`, `@tanstack/ai`, `@tanstack/ai-*`, `@ai-sdk/*`, `@ag-ui/*`, `@copilotkit/*`, `@anthropic-ai/*`, `@google/genai`, `@langchain/*`, `@mastra/*` and their subpaths. The message points an App or Widget at `useAction()` or `injectAction()`. The `application()` preset leaves them allowed for the shell's chat.
