<!-- intent-skills:start -->
# TanStack Intent - before editing files, run the matching guidance command.
tanstackIntent:
  - id: "@tecton/react#actions"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#actions"
    for: "Choose and use Tecton actions built on React Aria: Button with variant and size and onPress, LinkButton for navigation styled as a button, Link for inline links, ButtonGroup, Toggle, DropdownMenu and ContextMenu, CopyButton, ActionBar for a selection or unsaved changes. Covers onPress versus onClick, isDisabled, data-icon spacing, icon-only buttons with aria-label and the FAB recipe. Load when adding a button, link, menu, toolbar, toggle or action bar."
  - id: "@tecton/react#choose-component"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#choose-component"
    for: "The decision table for @tecton/react: which component to reach for across labels, feedback, overlays, selection, actions, progress, surfaces, forms, navigation, data, layout, presentation, conversation, infrastructure. Load it when the UI behaviour is clear but the component is not — it maps a need to one component, its import path and the family skill that carries the patterns."
  - id: "@tecton/react#conversation"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#conversation"
    for: "Build Tecton chat and assistant surfaces: Message with MessageContent, MessageFooter, MessageHeader and MessageAvatar for assistant, user and tool turns, MessageScroller with MessageScrollerProvider and MessageScrollerItem for the auto-following transcript, Bubble with BubbleContent and the align axis for compact chat bubbles, Attachment for files in a composer or a message, Questionnaire with QuestionnaireChoice and QuestionnaireSubmit for structured questions the assistant asks, Marker for full-width status rows and labelled dividers in a transcript. Load when building a chat panel, an AI assistant transcript, a composer, message attachments or transcript status rows."
  - id: "@tecton/react#core"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#core"
    for: "Foundation for writing any UI with @tecton/react. Covers the import paths (@tecton/react/components/<name>, @tecton/react/tecton/<name>, @tecton/react/icons — there is no root export), the @tecton/react/globals.css stylesheet and the dark-first `dark` class, why components are never installed with `shadcn add` and why files under components/ui are the wrong base, the reset Tailwind palette where bg-red-500 and text-zinc-400 emit no CSS at all, the semantic tokens (bg-primary, text-muted-foreground, text-destructive, bg-warning-surface) and the Tecton palette steps (bg-blue-120, text-blue-830), the rule that variants own colour, shape, size and padding while className carries layout only, the Tecton variant axes on Alert, Badge, Separator, Input, Textarea and SelectTrigger, data-icon=\"inline-start\" spacing, and Tecton domain glyphs versus Lucide. Load before writing or editing any Tecton markup, className, import or stylesheet."
  - id: "@tecton/react#data"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#data"
    for: "Choose and use Tecton data display: Table with TableHeader, TableRow and TableCell plus the TanStack Table recipe for sorting, filtering and pagination, TreeView for hierarchies with folder and item rows, Chart with ChartContainer, ChartTooltip and ChartLegend over Recharts, Stat with StatValue and StatDelta for KPI readouts, ColorSwatch for colour previews, Calendar and the date picker recipe. Load when rendering rows, a data grid, a tree, a chart, a KPI, a colour value or a date."
  - id: "@tecton/react#feedback"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#feedback"
    for: "Choose and use Tecton feedback: Alert with variant (success, warning, info, destructive) and appearance for messages that stay on the page, toast from sonner for transient confirmations, Empty for empty states, FieldError for validation. Load when showing a status message, banner, notification, error, warning, success or empty result."
  - id: "@tecton/react#forms"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#forms"
    for: "Build Tecton forms with React Aria controls: Field with FieldLabel, FieldDescription and FieldError around Input, Textarea, Select, Checkbox, Switch, Slider, InputGroup and InputOTP. Covers isInvalid and data-invalid, isSelected and onChange instead of checked and onCheckedChange, value and onChange on text fields, the outline, filled and text variants, and TanStack Form wiring. Load when building a form, input, text field, checkbox, switch, slider or validation message."
  - id: "@tecton/react#infrastructure"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#infrastructure"
    for: "Wire the Tecton providers and roots: DirectionProvider for right-to-left layouts, PortalProvider and usePortalTarget for choosing where overlays portal in pages with several React roots, ThemeRoot for an independently deployed application with its own scope class, mode and overlay container, ShortcutsProvider and useShortcut for the keyboard shortcut registry the shell lists. Load when mounting an application, a micro-frontend, an RTL locale, overlay portals or keyboard shortcuts."
  - id: "@tecton/react#labels"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#labels"
    for: "Choose and use Tecton labels: Badge for static status or category labels with variant and appearance, Chip and ChipGroup for selectable or removable tags, CountBadge for a count or dot anchored to an icon or avatar, Kbd for keyboard keys. Load when rendering a status, tag, pill, chip, label, counter or shortcut hint, or when tempted to colour a label with className."
  - id: "@tecton/react#layout"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#layout"
    for: "Choose and use Tecton layout and disclosure: Accordion for stacked expandable sections, Collapsible for one toggled region with Button slot trigger, Resizable panels with ResizablePanelGroup, ResizablePanel and ResizableHandle, ScrollArea for styled scrolling, AspectRatio for media boxes. Covers orientation instead of direction, pixel versus percent sizes, and the React Aria disclosure props instead of the Radix ones. Load when laying out expandable, split, scrolling or fixed-ratio regions."
  - id: "@tecton/react#navigation"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#navigation"
    for: "Choose and use Tecton navigation: Breadcrumb for the path to the current page, Sidebar with SidebarProvider, SidebarMenu and SidebarMenuButton for the application navigation rail, Pagination for paged lists, AppFinder for switching applications from the shell header. Load when building navigation, a breadcrumb trail, a sidebar, paging controls or an app switcher."
  - id: "@tecton/react#overlays"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#overlays"
    for: "Choose and use Tecton overlays built on React Aria: Dialog inside DialogTrigger for modal decisions and short forms, AlertDialog for destructive confirmations, Sheet for side panels, Drawer for bottom sheets, Popover for anchored content, HoverCard for previews, Tooltip for labels. Covers isOpen and onOpenChange, DialogTrigger composition and portal targets. Load when opening a modal, dialog, sheet, drawer, popover, tooltip or confirmation."
  - id: "@tecton/react#presentation"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#presentation"
    for: "Choose and use Tecton presentation surfaces: Carousel with CarouselContent and CarouselItem for slides, Avatar with AvatarImage, AvatarFallback and AvatarBadge, Overflow with OverflowItem and OverflowLabel for toolbars that collapse into a More menu, Canvas with CanvasSurface and its floating chrome slots for a full-bleed work surface, Background for the eleven decorative themed effects. Load when building slides, avatars, a collapsing toolbar, a map or canvas surface, or a decorative background."
  - id: "@tecton/react#progress"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#progress"
    for: "Choose and use Tecton progress and loading indicators: Progress for a determinate bar, CircularProgress for a ring or indeterminate spinner with a label, Meter for a segmented gauge of risk, complexity or confidence, Spinner inside buttons and inline, Skeleton for loading placeholders. Load when showing loading, progress, percentage, a gauge, a score or a placeholder."
  - id: "@tecton/react#react-aria"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#react-aria"
    for: "The Radix-to-React-Aria prop map for @tecton/react, because an agent's prior is shadcn on Radix and most of these substitutions render a dead control rather than an error. Covers onPress instead of onClick, isDisabled instead of disabled, isSelected and onChange instead of checked and onCheckedChange, selectedKey / defaultSelectedKey / onSelectionChange instead of value and onValueChange on Select and Tabs, isOpen and onOpenChange with DialogTrigger wrapping both the Button and the Dialog (there is no DialogContent), the absence of asChild and what replaces it (LinkButton, buttonVariants, the render prop on Badge and ButtonGroupText, the element-valued render on DrawerTrigger), id instead of value on SelectItem, TabsTrigger and TabsContent, isInvalid on React Aria controls versus aria-invalid on Input and Textarea plus data-invalid on Field, and why Link and LinkButton exist. Load before writing any prop, handler or controlled state on a Tecton component."
  - id: "@tecton/react#selection"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#selection"
    for: "Choose and use Tecton selection controls built on React Aria: Select with SelectTrigger, SelectValue, SelectContent and SelectItem, Combobox for searchable lists, NativeSelect for plain forms, RadioGroup, ToggleGroup for segmented choices, Tabs with selectedKey, Command for palettes. Covers selectedKey, onSelectionChange, id on items and the variant surfaces. Load when building a dropdown, select, picker, autocomplete, radio, segmented control, tabs or command menu."
  - id: "@tecton/react#surfaces"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#surfaces"
    for: "Choose and use Tecton surfaces: Card with CardHeader, CardContent and CardFooter for content, Panel for a titled application surface with scrolling body and pinned footer, Item for list rows, PageHeader for the title block with eyebrow, description, tabs and actions, AppShell for the application frame with its global header action cluster (command palette trigger, icon actions, overflow and user menu), Separator with emphasis. Load when laying out a page, panel, card, section, list row, header, the shell header actions or a divider."
  - id: "@tecton/react#theming"
    run: "pnpm dlx @tanstack/intent@latest load @tecton/react#theming"
    for: "How colour reaches the screen in @tecton/react and what an application may and may not change. Covers the shadcn CSS variables (--background, --primary, --ring, --success-surface, --border-strong) as the only carrier of the theme and their generation from tokens/tecton.map.json, why an application never declares a --color-* and a missing colour is a token request, the fifteen contrast ramps and how to read a step (bg-blue-120 text-blue-830, steps 50 to 1570, no dark: pair), dark-first with the dark class on html, inverting a section with the light or dark class, ThemeRoot with theme=\"inherit\" | \"dark\" | \"light\" for an independently deployed application, and micro-frontends with @tecton/react/styles/scoped.css, the @tecton/react/postcss/scope plugin and @tecton/react/federation/shared. Load when picking a colour, adding a token, toggling or inverting the mode, retinting a subtree, or mounting one Tecton application inside another."
<!-- intent-skills:end -->
