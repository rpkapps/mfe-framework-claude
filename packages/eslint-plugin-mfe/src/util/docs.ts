/** Documentation URL for a rule in this plugin, so a report is traceable. */
export function docsUrl(ruleName: string): string {
  return `https://github.com/company/mfe-framework/blob/main/packages/eslint-plugin-mfe/README.md#mfe${ruleName}`
}
